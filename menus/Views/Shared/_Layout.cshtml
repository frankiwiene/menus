<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - 🔥 Infierno</title>
    <script type="importmap"></script>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/menus.styles.css" asp-append-version="true" />
    <style>
        :root {
            --hellfire-red: #e63900;
            --abyss-black: #34495e;
            --soul-white: #f8f9fa;
            --ash-gray: #2b2b2b;
            --lava-orange: #ff5e00;
        }

        body {
            background-color: var(--abyss-black);
            color: var(--soul-white);
            min-height: 100vh;
            display: flex;
            flex-direction: column;
        }

        .navbar {
            background-color: var(--abyss-black) !important;
            border-bottom: 2px solid var(--hellfire-red) !important;
            box-shadow: 0 0 15px rgba(230, 57, 0, 0.3);
        }

        .navbar-brand {
            color: var(--hellfire-red) !important;
            font-weight: bold;
            font-size: 1.5rem;
            text-shadow: 0 0 5px rgba(230, 57, 0, 0.5);
        }

        .nav-link {
            color: var(--soul-white) !important;
            position: relative;
            margin: 0 5px;
            transition: all 0.3s ease;
        }

            .nav-link:hover {
                color: var(--hellfire-red) !important;
                transform: translateY(-2px);
            }

            .nav-link::before {
                content: "";
                position: absolute;
                width: 0;
                height: 2px;
                bottom: 0;
                left: 0;
                background-color: var(--hellfire-red);
                transition: width 0.3s ease;
            }

            .nav-link:hover::before {
                width: 100%;
            }

        b {
            color: var(--hellfire-red);
            text-shadow: 0 0 5px rgba(230, 57, 0, 0.5);
        }

        .container {
            flex: 1;
        }

        footer {
            background-color: var(--abyss-black);
            border-top: 2px solid var(--hellfire-red) !important;
            color: var(--soul-white);
            padding: 1rem 0;
            margin-top: auto;
        }

            footer a {
                color: var(--hellfire-red);
                text-decoration: none;
            }

                footer a:hover {
                    text-decoration: underline;
                    color: var(--lava-orange);
                }

        .diablo-message {
            background-color: white;
            border-left: 4px solid var(--hellfire-red);
            padding: 10px;
            margin: 10px 0;
            text-align: center;
            font-style: italic;
            animation: pulse 3s infinite;
        }


        {
            opacity: 0.8;
        }

        50% {
            opacity: 1;
        }

        100% {
            opacity: 0.8;
        }

        }

        .active-nav-item {
            color: var(--hellfire-red) !important;
            font-weight: bold;
        }

        .navbar-toggler {
            border-color: rgba(230, 57, 0, 0.5);
        }

        .navbar-toggler-icon {
            background-image: url("data:image/svg+xml,%3csvg viewBox='0 0 30 30' xmlns='http://www.w3.org/2000/svg'%3e%3cpath stroke='rgba(230, 57, 0, 0.8)' stroke-width='2' stroke-linecap='round' stroke-miterlimit='10' d='M4 7h22M4 15h22M4 23h22'/%3e%3c/svg%3e");
        }
    </style>
</head>
<body>
    <header>
        <nav class="navbar navbar-expand-sm navbar-toggleable-sm border-bottom box-shadow mb-3">
            <div class="container-fluid">
                <a class="navbar-brand" asp-area="" asp-controller="Home" asp-action="Index">🔥 INFIERNO</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target=".navbar-collapse" aria-controls="navbarSupportedContent"
                        aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="navbar-collapse collapse d-sm-inline-flex justify-content-between">
                    <ul class="navbar-nav flex-grow-1">
                        <li class="nav-item">
                            @if (ViewData["Title"] == "Home")
                            {
                                <b class="nav-link active-nav-item">Home</b>
                            }
                            else
                            {
                                <a class="nav-link" asp-area="" asp-controller="Home" asp-action="Index">Home</a>
                            }
                        </li>
                        <li class="nav-item">
                            @if (ViewData["Title"] == "Privacy")
                            {
                                <b class="nav-link active-nav-item">Privacy</b>
                            }
                            else
                            {
                                <a class="nav-link" asp-area="" asp-controller="Home" asp-action="Privacy">Privacy</a>
                            }
                        </li>
                        <li class="nav-item">
                            @if (ViewData["Title"] == "Photos")
                            {
                                <b class="nav-link active-nav-item">Photos</b>
                            }
                            else
                            {
                                <a class="nav-link" asp-area="" asp-controller="Home" asp-action="Photos">Photos</a>
                            }
                        </li>
                        <li class="nav-item">
                            @if (ViewData["Title"] == "Contact")
                            {
                                <b class="nav-link active-nav-item">Contact</b>
                            }
                            else
                            {
                                <a class="nav-link" asp-area="" asp-controller="Home" asp-action="Contact">Contact</a>
                            }


                        </li>
                        <li class="nav-item">
                            @if (ViewData["Title"] == "GetPokemon")
                            {
                                <b class="nav-link active-nav-item">GetPokemon</b>
                            }
                            else
                            {
                                <a class="nav-link" href="https://localhost:7172/home/GetPokemon/pikachu">Pokémon</a>
                            }
                        </li>

                        <li class="nav-item">
                            @if (ViewData["Title"] == "GetJoke")
                            {
                                <b class="nav-link active-nav-item">GetJoke</b>
                            }
                            else
                            {
                                <a class="nav-link" href="https://localhost:7172/home/Login">BROMAaaaa</a>
                            }


                        </li>


                    </ul>
                </div>
            </div>
        </nav>
    </header>

    <div class="container">
        <main role="main" class="pb-3">
            @RenderBody()
        </main>
    </div>

    <footer class="border-top footer text-muted">
        <div class="diablo-message">
            <b>HOLA SOY EL DIABLO Y AQUÍ MANDO YO, ESTA ACTIVIDAD ES UN COLLAB DE VARIAS ACTIVIDADES</b>
        </div>
        <div class="container text-center">
            &copy; 2025 - 🔥 INFIERNO - <a asp-area="" asp-controller="Home" asp-action="Privacy">Privacy</a>
        </div>
    </footer>

    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>